<h1>Items Index Page</h1>
<% @items.each do |item| %>
	<li><%=item.print_item %></li>
	<% if session[:id] %>
		<%= render partial: "/logs/form", locals: {user: @user, item: item} %>
	<% end %>
<% end %>